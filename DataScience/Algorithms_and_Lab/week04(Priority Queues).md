### Priority Queues

- MAX-HEAP-EXTRACT-MAX(A)
    1. Exchange the numbers between the root and the last
    2. Remove the last child
    3. MAX-HEAPIFY
- MAX-HEAP-INCREASE-KEY(A,x,k)
    - put a key into a
- MAX-HEAP-INSERT(A,x,n)
- Quadratic: Insertion Sort, Selection Sort
- Log Linear: Heap Sort, Merge Sort
